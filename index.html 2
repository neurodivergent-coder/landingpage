<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DELULU.exe - Terminal Creativo</title>
    <link href="https://fonts.googleapis.com/css2?family=Courier+Prime:ital,wght@0,400;0,700;1,400&family=Share+Tech+Mono&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Share Tech Mono', 'Courier Prime', monospace;
            background: #000000;
            color: #00ff41;
            line-height: 1.6;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }

        /* Cyberpunk background effects */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(90deg, transparent 98%, #00ff41 100%),
                linear-gradient(180deg, transparent 98%, #00ff41 100%);
            background-size: 50px 50px;
            opacity: 0.03;
            z-index: 1;
            animation: grid-move 20s linear infinite;
        }

        @keyframes grid-move {
            0% { transform: translate(0, 0); }
            100% { transform: translate(50px, 50px); }
        }

        /* Scanning line effect */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: linear-gradient(90deg, transparent, #00ff41, transparent);
            z-index: 1000;
            animation: scan 3s linear infinite;
        }

        @keyframes scan {
            0% { top: 0; opacity: 1; }
            100% { top: 100vh; opacity: 0; }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            z-index: 2;
        }

        /* Header */
        header {
            padding: 20px 0;
            border-bottom: 2px solid #00ff41;
            margin-bottom: 60px;
            position: sticky;
            top: 0;
            background: rgba(0, 0, 0, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            box-shadow: 0 0 20px rgba(0, 255, 65, 0.3);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .brand {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        .logo {
            font-family: 'Share Tech Mono', monospace;
            font-size: 3rem;
            font-weight: 700;
            color: #00ff41;
            margin-bottom: 5px;
            text-shadow: 0 0 10px #00ff41;
            animation: glitch 2s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% { transform: translate(0); }
            20% { transform: translate(-2px, 2px); }
            40% { transform: translate(-2px, -2px); }
            60% { transform: translate(2px, 2px); }
            80% { transform: translate(2px, -2px); }
        }

        .tagline {
            font-size: 1rem;
            color: #00ffff;
            font-style: italic;
            letter-spacing: 2px;
            text-transform: uppercase;
            text-shadow: 0 0 5px #00ffff;
        }

        /* Navigation */
        .navbar {
            display: flex;
            align-items: center;
            gap: 40px;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 30px;
            margin: 0;
            padding: 0;
        }

        .nav-menu li a {
            color: #00ff41;
            text-decoration: none;
            font-weight: 400;
            font-size: 1rem;
            transition: all 0.3s ease;
            padding: 8px 15px;
            border: 1px solid transparent;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .nav-menu li a:hover,
        .nav-menu li a.active {
            border: 1px solid #00ff41;
            background: rgba(0, 255, 65, 0.1);
            color: #00ffff;
            text-shadow: 0 0 10px #00ffff;
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
        }

        /* Social Icons */
        .social-icons {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        .social-icon {
            width: 40px;
            height: 40px;
            border: 2px solid #00ff41;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            color: #00ff41;
            font-weight: 700;
            font-size: 0.8rem;
            transition: all 0.3s ease;
            background: rgba(0, 0, 0, 0.8);
            text-shadow: 0 0 5px #00ff41;
        }

        .social-icon:hover {
            background: #00ff41;
            color: #000000;
            box-shadow: 0 0 20px #00ff41;
            transform: scale(1.1);
        }

        /* Mobile Menu */
        .mobile-menu-toggle {
            display: none;
            flex-direction: column;
            cursor: pointer;
            padding: 5px;
        }

        .hamburger {
            width: 25px;
            height: 3px;
            background: #00ff41;
            margin: 3px 0;
            transition: 0.3s;
            box-shadow: 0 0 5px #00ff41;
        }

        .mobile-menu-toggle.active .hamburger:nth-child(1) {
            transform: rotate(-45deg) translate(-5px, 6px);
        }

        .mobile-menu-toggle.active .hamburger:nth-child(2) {
            opacity: 0;
        }

        .mobile-menu-toggle.active .hamburger:nth-child(3) {
            transform: rotate(45deg) translate(-5px, -6px);
        }

        .mobile-menu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            width: 100%;
            background: rgba(0, 0, 0, 0.95);
            border-top: 2px solid #00ff41;
            padding: 20px;
            flex-direction: column;
            gap: 20px;
            backdrop-filter: blur(10px);
        }

        .mobile-menu.active {
            display: flex;
        }

        .mobile-nav-menu {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .mobile-nav-menu li a {
            color: #00ff41;
            text-decoration: none;
            font-weight: 400;
            font-size: 1.1rem;
            padding: 12px;
            border: 1px solid #00ff41;
            display: block;
            text-align: center;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            background: rgba(0, 0, 0, 0.5);
        }

        .mobile-nav-menu li a:hover,
        .mobile-nav-menu li a.active {
            background: rgba(0, 255, 65, 0.2);
            color: #00ffff;
            text-shadow: 0 0 10px #00ffff;
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
        }

        .mobile-social {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        /* Hero Section */
        .hero {
            text-align: center;
            margin-bottom: 80px;
            padding: 40px;
            background: rgba(0, 0, 0, 0.8);
            border: 2px solid #00ff41;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(0, 255, 65, 0.1), transparent);
            animation: hero-scan 4s ease-in-out infinite;
        }

        @keyframes hero-scan {
            0% { left: -100%; }
            50% { left: 100%; }
            100% { left: -100%; }
        }

        .hero h1 {
            font-size: 2.5rem;
            margin-bottom: 30px;
            color: #00ffff;
            font-weight: 700;
            text-shadow: 0 0 20px #00ffff;
            position: relative;
            z-index: 2;
        }

        .hero p {
            font-size: 1.2rem;
            max-width: 800px;
            margin: 0 auto;
            color: #00ff41;
            line-height: 1.8;
            position: relative;
            z-index: 2;
        }

        /* Services Grid */
        .services {
            margin-bottom: 80px;
        }

        .services h2 {
            font-family: 'Share Tech Mono', monospace;
            font-size: 3rem;
            text-align: center;
            margin-bottom: 50px;
            color: #00ffff;
            font-weight: 700;
            text-shadow: 0 0 20px #00ffff;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 30px;
        }

        .service-card {
            background: rgba(0, 0, 0, 0.8);
            padding: 30px;
            border: 2px solid #00ff41;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(45deg, transparent 30%, rgba(0, 255, 65, 0.1) 50%, transparent 70%);
            transform: translateX(-100%);
            transition: transform 0.6s ease;
        }

        .service-card:hover::before {
            transform: translateX(100%);
        }

        .service-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 40px rgba(0, 255, 65, 0.3);
            border-color: #00ffff;
            background: rgba(0, 20, 0, 0.9);
        }

        .service-card:hover .service-number {
            color: #ff0080;
            text-shadow: 0 0 15px #ff0080;
            animation: number-glitch 0.5s ease;
        }

        @keyframes number-glitch {
            0%, 100% { transform: translate(0); }
            25% { transform: translate(-2px, 2px); }
            75% { transform: translate(2px, -2px); }
        }

        .service-number {
            font-family: 'Share Tech Mono', monospace;
            font-size: 2rem;
            color: #00ff41;
            margin-bottom: 15px;
            font-weight: 700;
            text-shadow: 0 0 10px #00ff41;
            transition: all 0.3s ease;
        }

        .service-title {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 15px;
            color: #00ffff;
            text-shadow: 0 0 5px #00ffff;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .service-description {
            color: #00ff41;
            font-style: italic;
        }

        /* CTA Section */
        .cta {
            text-align: center;
            padding: 60px 40px;
            background: rgba(0, 0, 0, 0.9);
            border: 2px solid #00ffff;
            margin-bottom: 60px;
            position: relative;
            overflow: hidden;
        }

        .cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(0, 255, 65, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 50%, rgba(0, 255, 255, 0.1) 0%, transparent 50%);
            animation: cta-pulse 3s ease-in-out infinite alternate;
        }

        @keyframes cta-pulse {
            0% { opacity: 0.3; }
            100% { opacity: 0.7; }
        }

        .cta h2 {
            font-family: 'Share Tech Mono', monospace;
            font-size: 2.5rem;
            margin-bottom: 20px;
            color: #ff0080;
            font-weight: 700;
            text-shadow: 0 0 20px #ff0080;
            text-transform: uppercase;
            letter-spacing: 2px;
            position: relative;
            z-index: 2;
        }

        .cta p {
            font-size: 1.1rem;
            margin-bottom: 30px;
            color: #00ff41;
            position: relative;
            z-index: 2;
        }

        .cta-button {
            display: inline-block;
            padding: 15px 40px;
            background: rgba(0, 0, 0, 0.8);
            color: #00ff41;
            text-decoration: none;
            font-weight: 700;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            border: 2px solid #00ff41;
            text-transform: uppercase;
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, #00ff41, transparent);
            transition: left 0.5s ease;
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            background: #00ff41;
            color: #000000;
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(0, 255, 65, 0.4);
            text-shadow: none;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 40px 0;
            border-top: 2px solid #00ff41;
            color: #00ff41;
            font-style: italic;
            background: rgba(0, 0, 0, 0.9);
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .navbar {
                gap: 20px;
            }

            .nav-menu {
                gap: 20px;
            }

            .social-icons {
                gap: 8px;
            }
        }

        @media (max-width: 768px) {
            .header-content {
                position: relative;
            }

            .navbar {
                display: none;
            }

            .mobile-menu-toggle {
                display: flex;
            }

            .logo {
                font-size: 2.5rem;
            }

            .tagline {
                font-size: 0.9rem;
            }

            .hero {
                padding: 30px 20px;
            }

            .hero h1 {
                font-size: 2rem;
            }

            .hero p {
                font-size: 1.1rem;
            }

            .services h2 {
                font-size: 2.5rem;
            }

            .services-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .service-card {
                padding: 25px;
            }

            .cta {
                padding: 40px 20px;
            }

            .cta h2 {
                font-size: 2rem;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 0 15px;
            }

            .logo {
                font-size: 2rem;
            }

            .hero h1 {
                font-size: 1.8rem;
            }

            .hero p {
                font-size: 1rem;
            }

            .services h2 {
                font-size: 2rem;
            }

            .service-card {
                padding: 20px;
            }

            .service-number {
                font-size: 1.5rem;
            }

            .service-title {
                font-size: 1.1rem;
            }

            .cta {
                padding: 30px 15px;
            }

            .cta h2 {
                font-size: 1.8rem;
            }

            .cta-button {
                padding: 12px 30px;
                font-size: 0.9rem;
            }

            .social-icon {
                width: 32px;
                height: 32px;
                font-size: 0.7rem;
            }

            .mobile-social {
                flex-wrap: wrap;
                gap: 10px;
            }
        }

        /* Terminal cursor animation */
        .terminal-cursor {
            display: inline-block;
            background: #00ff41;
            width: 3px;
            animation: cursor-blink 1s infinite;
        }

        @keyframes cursor-blink {
            0%, 50% { opacity: 1; }
            51%, 100% { opacity: 0; }
        }

        /* Typewriter animation for hero text */
        .typewriter {
            overflow: hidden;
            border-right: 3px solid #00ffff;
            white-space: nowrap;
            margin: 0 auto;
            animation: typing 3s steps(40, end), blink-caret 0.75s step-end infinite;
        }

        @keyframes typing {
            from { width: 0 }
            to { width: 100% }
        }

        @keyframes blink-caret {
            from, to { border-color: transparent }
            50% { border-color: #00ffff }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="header-content">
                <div class="brand">
                    <div class="logo">DELULU.exe</div>
                    <div class="tagline">CREATIVE_TERMINAL</div>
                </div>
                
                <nav class="navbar">
                    <ul class="nav-menu">
                        <li><a href="#inicio">[HOME]</a></li>
                        <li><a href="#servicios">[SERVICES]</a></li>
                        <li><a href="#sobre-nosotros">[ABOUT]</a></li>
                        <li><a href="#contacto">[CONTACT]</a></li>
                    </ul>
                    
                    <div class="social-icons">
                        <a href="https://linkedin.com" target="_blank" class="social-icon" title="LinkedIn">IN</a>
                        <a href="https://github.com" target="_blank" class="social-icon" title="GitHub">GH</a>
                        <a href="https://pinterest.com" target="_blank" class="social-icon" title="Pinterest">PT</a>
                        <a href="https://tiktok.com" target="_blank" class="social-icon" title="TikTok">TK</a>
                        <a href="https://instagram.com" target="_blank" class="social-icon" title="Instagram">IG</a>
                    </div>
                </nav>
                
                <div class="mobile-menu-toggle">
                    <div class="hamburger"></div>
                    <div class="hamburger"></div>
                    <div class="hamburger"></div>
                </div>
            </div>
            
            <div class="mobile-menu">
                <ul class="mobile-nav-menu">
                    <li><a href="#inicio">[HOME]</a></li>
                    <li><a href="#servicios">[SERVICES]</a></li>
                    <li><a href="#sobre-nosotros">[ABOUT]</a></li>
                    <li><a href="#contacto">[CONTACT]</a></li>
                </ul>
                
                <div class="mobile-social">
                    <a href="https://linkedin.com" target="_blank" class="social-icon" title="LinkedIn">IN</a>
                    <a href="https://github.com" target="_blank" class="social-icon" title="GitHub">GH</a>
                    <a href="https://pinterest.com" target="_blank" class="social-icon" title="Pinterest">PT</a>
                    <a href="https://tiktok.com" target="_blank" class="social-icon" title="TikTok">TK</a>
                    <a href="https://instagram.com" target="_blank" class="social-icon" title="Instagram">IG</a>
                </div>
            </div>
        </header>

        <section id="inicio" class="hero">
            <h1 class="typewriter">> CREATIVIDAD_INFINITA.exe</h1>
            <p>DELULU.exe es un terminal creativo especializado en la integración de diseño digital, desarrollo de sistemas avanzados y análisis predictivo de tendencias para la creación de soluciones tecnológicas de vanguardia y estrategias de marca disruptivas.</p>
        </section>

        <section id="servicios" class="services">
            <h2>SERVICIOS_DISPONIBLES</h2>
            <div class="services-grid">
                <div class="service-card">
                    <div class="service-number">0x01</div>
                    <div class="service-title">UI/UX_DEVELOPMENT</div>
                    <div class="service-description">Desarrollamos interfaces digitales intuitivas y experiencias de usuario que conectan con tu audiencia objetivo mediante algoritmos de engagement.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x02</div>
                    <div class="service-title">QA_CONSULTING</div>
                    <div class="service-description">Garantizamos la excelencia en cada proyecto mediante protocolos rigurosos de aseguramiento de calidad y testing automatizado.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x03</div>
                    <div class="service-title">TREND_FORECASTING</div>
                    <div class="service-description">Anticipamos tendencias futuras utilizando análisis de big data, machine learning y tecnología de predicción avanzada.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x04</div>
                    <div class="service-title">BRAND_IDENTITY</div>
                    <div class="service-description">Construimos identidades digitales sólidas que reflejan la esencia única de tu marca en el ecosistema digital.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x05</div>
                    <div class="service-title">GRAPHIC_DESIGN</div>
                    <div class="service-description">Comunicamos visualmente tus ideas mediante diseños vectoriales impactantes y sistemas gráficos memorables.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x06</div>
                    <div class="service-title">SOCIAL_CONTENT</div>
                    <div class="service-description">Desarrollamos estrategias de contenido viral que generan engagement orgánico y construyen comunidades digitales.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x07</div>
                    <div class="service-title">DIGITAL_PHOTOGRAPHY</div>
                    <div class="service-description">Capturamos la esencia de tu marca a través de fotografía digital profesional y técnicas de post-procesamiento avanzado.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x08</div>
                    <div class="service-title">CREATIVE_WRITING</div>
                    <div class="service-description">Creamos narrativas digitales persuasivas que conectan emocionalmente con tu audiencia mediante storytelling algorítmico.</div>
                </div>

                <div class="service-card">
                    <div class="service-number">0x09</div>
                    <div class="service-title">DIGITAL_MAGAZINE</div>
                    <div class="service-description">Publicamos insights y tendencias emergentes a través de nuestra revista digital especializada en innovación tecnológica.</div>
                </div>
            </div>
        </section>

        <section id="contacto" class="cta">
            <h2>> INICIAR_PROYECTO.exe</h2>
            <p>Transformemos tus ideas en realidades digitales que marquen la diferencia en el ciberespacio.</p>
            <a href="#contacto" class="cta-button">EJECUTAR_COMANDO</a>
        </section>

        <footer>
            <p>© 2024 DELULU.exe - CREATIVE_TERMINAL | WHERE_IDEAS.exe COME_TO_LIFE</p>
        </footer>
    </div>

    <script>
        // Mobile menu toggle
        const mobileMenuToggle = document.querySelector('.mobile-menu-toggle');
        const mobileMenu = document.querySelector('.mobile-menu');

        mobileMenuToggle.addEventListener('click', function() {
            mobileMenuToggle.classList.toggle('active');
            mobileMenu.classList.toggle('active');
        });

        // Close mobile menu when clicking on a link
        const mobileNavLinks = document.querySelectorAll('.mobile-nav-menu a');
        mobileNavLinks.forEach(link => {
            link.addEventListener('click', function() {
                mobileMenuToggle.classList.remove('active');
                mobileMenu.classList.remove('active');
            });
        });

        // Smooth scrolling for navigation links
        const navLinks = document.querySelectorAll('a[href^="#"]');
        navLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetSection = document.querySelector(targetId);
                
                if (targetSection) {
                    const headerHeight = document.querySelector('header').offsetHeight;
                    const targetPosition = targetSection.offsetTop - headerHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Smooth scrolling for CTA button
        document.querySelector('.cta-button').addEventListener('click', function(e) {
            e.preventDefault();
            
            // Cyberpunk style alert
            alert('> CONEXIÓN_ESTABLECIDA\n> PROTOCOLO_CONTACTO.exe INICIADO\n> PRONTO IMPLEMENTAREMOS EL TERMINAL DE CONTACTO\n> MANTENTE_CONECTADO...');
        });

        // Enhanced parallax effect for service cards
        window.addEventListener('scroll', function() {
            const cards = document.querySelectorAll('.service-card');
            const scrolled = window.pageYOffset;
            
            cards.forEach((card, index) => {
                const rate = scrolled * -0.3;
                const yPos = -(rate / (index + 1));
                card.style.transform = `translate3d(0, ${yPos}px, 0)`;
            });
        });

        // Add typing effect completion callback
        setTimeout(() => {
            const typewriter = document.querySelector('.typewriter');
            if (typewriter) {
                typewriter.style.borderRight = 'none';
            }
        }, 4000);

        // Close mobile menu when clicking outside
        document.addEventListener('click', function(e) {
            if (!mobileMenuToggle.contains(e.target) && !mobileMenu.contains(e.target)) {
                mobileMenuToggle.classList.remove('active');
                mobileMenu.classList.remove('active');
            }
        });

        // Active navigation highlighting
        window.addEventListener('scroll', function() {
            const sections = document.querySelectorAll('section[id]');
            const navLinks = document.querySelectorAll('.nav-menu a, .mobile-nav-menu a');
            
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                if (pageYOffset >= (sectionTop - 200)) {
                    current = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === '#' + current) {
                    link.classList.add('active');
                }
            });
        });

        // Random glitch effect on service cards
        setInterval(() => {
            const cards = document.querySelectorAll('.service-card');
            const randomCard = cards[Math.floor(Math.random() * cards.length)];
            
            randomCard.style.transform += ' skew(1deg)';
            setTimeout(() => {
                randomCard.style.transform = randomCard.style.transform.replace(' skew(1deg)', '');
            }, 100);
        }, 5000);

        // Terminal boot sequence simulation
        window.addEventListener('load', function() {
            console.log('%c> DELULU.exe TERMINAL INICIADO', 'color: #00ff41; font-family: monospace; font-size: 14px;');
            console.log('%c> CARGANDO MÓDULOS CREATIVOS...', 'color: #00ffff; font-family: monospace; font-size: 12px;');
            console.log('%c> SISTEMA LISTO PARA CREAR', 'color: #ff0080; font-family: monospace; font-size: 12px;');
        });
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'994eee32c6dbf6a5',t:'MTc2MTUzMzg5My4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
