<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DELULU INVADERS - Arcade Studio</title>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Press+Start+2P&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Press Start 2P', monospace;
            background: #000011;
            color: #00ff00;
            line-height: 1.6;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }

        /* Starfield background */
        .starfield {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .star {
            position: absolute;
            background: #ffffff;
            border-radius: 50%;
            animation: twinkle 2s infinite alternate;
        }

        .star:nth-child(odd) {
            animation-delay: 1s;
        }

        @keyframes twinkle {
            0% { opacity: 0.3; transform: scale(1); }
            100% { opacity: 1; transform: scale(1.2); }
        }

        /* CRT Screen effect */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(transparent 50%, rgba(0, 255, 0, 0.03) 50%),
                linear-gradient(90deg, transparent 50%, rgba(255, 0, 0, 0.01) 50%);
            background-size: 100% 4px, 4px 100%;
            pointer-events: none;
            z-index: 1000;
        }

        /* Scanlines */
        body::after {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(
                transparent 0%,
                rgba(0, 255, 0, 0.1) 50%,
                transparent 100%
            );
            background-size: 100% 3px;
            animation: scanlines 0.1s linear infinite;
            pointer-events: none;
            z-index: 999;
        }

        @keyframes scanlines {
            0% { transform: translateY(0); }
            100% { transform: translateY(3px); }
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            position: relative;
            z-index: 2;
        }

        /* Header */
        header {
            padding: 20px 0;
            border-bottom: 3px solid #00ff00;
            margin-bottom: 40px;
            position: sticky;
            top: 0;
            background: rgba(0, 0, 17, 0.95);
            z-index: 1000;
            box-shadow: 0 0 20px rgba(0, 255, 0, 0.5);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .brand {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        .logo {
            font-family: 'Press Start 2P', monospace;
            font-size: 2.5rem;
            font-weight: 700;
            color: #ffff00;
            margin-bottom: 8px;
            text-shadow: 
                0 0 5px #ffff00,
                0 0 10px #ffff00,
                0 0 15px #ffff00;
            animation: logo-glow 2s ease-in-out infinite alternate;
        }

        @keyframes logo-glow {
            0% { text-shadow: 0 0 5px #ffff00, 0 0 10px #ffff00, 0 0 15px #ffff00; }
            100% { text-shadow: 0 0 10px #ffff00, 0 0 20px #ffff00, 0 0 30px #ffff00; }
        }

        .tagline {
            font-size: 0.8rem;
            color: #00ffff;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .score-display {
            font-size: 1rem;
            color: #ff0080;
            margin-top: 5px;
        }

        /* Navigation */
        .navbar {
            display: flex;
            align-items: center;
            gap: 30px;
        }

        .nav-menu {
            display: flex;
            list-style: none;
            gap: 20px;
            margin: 0;
            padding: 0;
        }

        .nav-menu li a {
            color: #00ff00;
            text-decoration: none;
            font-size: 0.8rem;
            transition: all 0.3s ease;
            padding: 8px 12px;
            border: 2px solid transparent;
            text-transform: uppercase;
        }

        .nav-menu li a:hover,
        .nav-menu li a.active {
            border: 2px solid #00ff00;
            background: rgba(0, 255, 0, 0.1);
            color: #ffff00;
            text-shadow: 0 0 10px #ffff00;
            transform: scale(1.1);
        }

        /* Social Icons - Styled as game elements */
        .social-icons {
            display: flex;
            gap: 10px;
            align-items: center;
        }

        .social-icon {
            width: 35px;
            height: 35px;
            border: 2px solid #00ff00;
            display: flex;
            align-items: center;
            justify-content: center;
            text-decoration: none;
            color: #00ff00;
            font-size: 0.6rem;
            transition: all 0.3s ease;
            background: rgba(0, 0, 0, 0.8);
            position: relative;
        }

        .social-icon::before {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: linear-gradient(45deg, #00ff00, #ffff00, #ff0080, #00ffff);
            z-index: -1;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .social-icon:hover::before {
            opacity: 1;
        }

        .social-icon:hover {
            background: #00ff00;
            color: #000000;
            transform: scale(1.2) rotate(360deg);
            animation: power-up 0.5s ease;
        }

        @keyframes power-up {
            0% { transform: scale(1); }
            50% { transform: scale(1.3); }
            100% { transform: scale(1.2); }
        }

        /* Mobile Menu */
        .mobile-menu-toggle {
            display: none;
            flex-direction: column;
            cursor: pointer;
            padding: 5px;
        }

        .hamburger {
            width: 20px;
            height: 3px;
            background: #00ff00;
            margin: 2px 0;
            transition: 0.3s;
            box-shadow: 0 0 5px #00ff00;
        }

        .mobile-menu {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            width: 100%;
            background: rgba(0, 0, 17, 0.95);
            border-top: 3px solid #00ff00;
            padding: 20px;
            flex-direction: column;
            gap: 15px;
        }

        .mobile-menu.active {
            display: flex;
        }

        .mobile-nav-menu {
            list-style: none;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .mobile-nav-menu li a {
            color: #00ff00;
            text-decoration: none;
            font-size: 0.8rem;
            padding: 10px;
            border: 2px solid #00ff00;
            display: block;
            text-align: center;
            transition: all 0.3s ease;
            text-transform: uppercase;
        }

        .mobile-social {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-top: 15px;
        }

        /* Hero Section - Game Screen */
        .hero {
            text-align: center;
            margin-bottom: 60px;
            padding: 40px;
            background: rgba(0, 0, 17, 0.8);
            border: 3px solid #00ff00;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 25% 25%, rgba(255, 255, 0, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 75% 75%, rgba(0, 255, 255, 0.1) 0%, transparent 50%);
            animation: radar-sweep 4s linear infinite;
        }

        @keyframes radar-sweep {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .hero h1 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #ffff00;
            font-weight: 700;
            text-shadow: 0 0 20px #ffff00;
            position: relative;
            z-index: 2;
            animation: title-pulse 3s ease-in-out infinite;
        }

        @keyframes title-pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .hero p {
            font-size: 0.9rem;
            max-width: 800px;
            margin: 0 auto;
            color: #00ff00;
            line-height: 1.8;
            position: relative;
            z-index: 2;
        }

        .game-stats {
            display: flex;
            justify-content: center;
            gap: 40px;
            margin-top: 20px;
            font-size: 0.8rem;
        }

        .stat {
            color: #ff0080;
        }

        /* Services Grid - Invader Formation */
        .services {
            margin-bottom: 60px;
        }

        .services h2 {
            font-family: 'Press Start 2P', monospace;
            font-size: 2rem;
            text-align: center;
            margin-bottom: 40px;
            color: #ff0080;
            font-weight: 700;
            text-shadow: 0 0 20px #ff0080;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            max-width: 900px;
            margin: 0 auto;
        }

        .service-card {
            background: rgba(0, 0, 17, 0.9);
            padding: 25px;
            border: 3px solid #00ff00;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            cursor: crosshair;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: radial-gradient(circle, rgba(255, 255, 0, 0.3) 0%, transparent 70%);
            transform: translate(-50%, -50%);
            transition: all 0.5s ease;
        }

        .service-card:hover::before {
            width: 200%;
            height: 200%;
        }

        .service-card:hover {
            transform: translateY(-10px) scale(1.05);
            border-color: #ffff00;
            background: rgba(0, 20, 0, 0.9);
            animation: invader-hit 0.5s ease;
        }

        @keyframes invader-hit {
            0% { transform: translateY(-10px) scale(1.05); }
            25% { transform: translateY(-10px) scale(1.1) rotate(2deg); }
            75% { transform: translateY(-10px) scale(1.1) rotate(-2deg); }
            100% { transform: translateY(-10px) scale(1.05); }
        }

        .service-number {
            font-family: 'Press Start 2P', monospace;
            font-size: 1.2rem;
            color: #ffff00;
            margin-bottom: 15px;
            font-weight: 700;
            text-shadow: 0 0 10px #ffff00;
        }

        .service-title {
            font-size: 0.9rem;
            font-weight: 700;
            margin-bottom: 15px;
            color: #00ffff;
            text-transform: uppercase;
            letter-spacing: 1px;
            line-height: 1.4;
        }

        .service-description {
            color: #00ff00;
            font-size: 0.7rem;
            line-height: 1.6;
        }

        .service-points {
            position: absolute;
            top: 10px;
            right: 15px;
            color: #ff0080;
            font-size: 0.7rem;
        }

        /* CTA Section - Boss Battle */
        .cta {
            text-align: center;
            padding: 50px 30px;
            background: rgba(0, 0, 17, 0.95);
            border: 3px solid #ff0080;
            margin-bottom: 40px;
            position: relative;
            overflow: hidden;
        }

        .cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 0, 128, 0.2), transparent);
            animation: boss-laser 3s linear infinite;
        }

        @keyframes boss-laser {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        .cta h2 {
            font-family: 'Press Start 2P', monospace;
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #ff0080;
            font-weight: 700;
            text-shadow: 0 0 20px #ff0080;
            text-transform: uppercase;
            position: relative;
            z-index: 2;
        }

        .cta p {
            font-size: 0.8rem;
            margin-bottom: 25px;
            color: #00ff00;
            position: relative;
            z-index: 2;
            line-height: 1.6;
        }

        .cta-button {
            display: inline-block;
            padding: 15px 30px;
            background: rgba(0, 0, 0, 0.8);
            color: #ffff00;
            text-decoration: none;
            font-size: 0.8rem;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            border: 3px solid #ffff00;
            text-transform: uppercase;
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: #ffff00;
            transition: left 0.3s ease;
            z-index: -1;
        }

        .cta-button:hover::before {
            left: 0;
        }

        .cta-button:hover {
            color: #000000;
            transform: scale(1.1);
            animation: button-flash 0.5s ease;
        }

        @keyframes button-flash {
            0%, 100% { box-shadow: 0 0 10px #ffff00; }
            50% { box-shadow: 0 0 30px #ffff00, 0 0 50px #ffff00; }
        }

        /* Footer - Game Over Screen */
        footer {
            text-align: center;
            padding: 30px 0;
            border-top: 3px solid #00ff00;
            color: #00ff00;
            background: rgba(0, 0, 17, 0.9);
            font-size: 0.7rem;
        }

        .footer-score {
            color: #ffff00;
            margin-bottom: 10px;
            font-size: 0.8rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .navbar {
                display: none;
            }

            .mobile-menu-toggle {
                display: flex;
            }

            .logo {
                font-size: 1.8rem;
            }

            .hero h1 {
                font-size: 1.5rem;
            }

            .hero p {
                font-size: 0.8rem;
            }

            .services h2 {
                font-size: 1.5rem;
            }

            .services-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            .service-card {
                padding: 20px;
            }

            .game-stats {
                flex-direction: column;
                gap: 10px;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 0 15px;
            }

            .logo {
                font-size: 1.5rem;
            }

            .hero {
                padding: 25px 15px;
            }

            .hero h1 {
                font-size: 1.2rem;
            }

            .hero p {
                font-size: 0.7rem;
            }

            .services h2 {
                font-size: 1.2rem;
            }

            .cta {
                padding: 30px 15px;
            }

            .cta h2 {
                font-size: 1.3rem;
            }

            .social-icon {
                width: 28px;
                height: 28px;
                font-size: 0.5rem;
            }
        }

        /* Pixel perfect animations */
        .pixel-perfect {
            image-rendering: -moz-crisp-edges;
            image-rendering: -webkit-crisp-edges;
            image-rendering: pixelated;
            image-rendering: crisp-edges;
        }
    </style>
</head>
<body>
    <div class="starfield"></div>
    
    <div class="container">
        <header>
            <div class="header-content">
                <div class="brand">
                    <div class="logo">DELULU INVADERS</div>
                    <div class="tagline">ARCADE STUDIO</div>
                    <div class="score-display">HIGH SCORE: 999999</div>
                </div>
                
                <nav class="navbar">
                    <ul class="nav-menu">
                        <li><a href="#inicio">START</a></li>
                        <li><a href="#servicios">LEVELS</a></li>
                        <li><a href="#sobre-nosotros">ABOUT</a></li>
                        <li><a href="#contacto">CONTACT</a></li>
                    </ul>
                    
                    <div class="social-icons">
                        <a href="https://linkedin.com" target="_blank" class="social-icon" title="LinkedIn">IN</a>
                        <a href="https://github.com" target="_blank" class="social-icon" title="GitHub">GH</a>
                        <a href="https://pinterest.com" target="_blank" class="social-icon" title="Pinterest">PT</a>
                        <a href="https://tiktok.com" target="_blank" class="social-icon" title="TikTok">TK</a>
                        <a href="https://instagram.com" target="_blank" class="social-icon" title="Instagram">IG</a>
                    </div>
                </nav>
                
                <div class="mobile-menu-toggle">
                    <div class="hamburger"></div>
                    <div class="hamburger"></div>
                    <div class="hamburger"></div>
                </div>
            </div>
            
            <div class="mobile-menu">
                <ul class="mobile-nav-menu">
                    <li><a href="#inicio">START</a></li>
                    <li><a href="#servicios">LEVELS</a></li>
                    <li><a href="#sobre-nosotros">ABOUT</a></li>
                    <li><a href="#contacto">CONTACT</a></li>
                </ul>
                
                <div class="mobile-social">
                    <a href="https://linkedin.com" target="_blank" class="social-icon" title="LinkedIn">IN</a>
                    <a href="https://github.com" target="_blank" class="social-icon" title="GitHub">GH</a>
                    <a href="https://pinterest.com" target="_blank" class="social-icon" title="Pinterest">PT</a>
                    <a href="https://tiktok.com" target="_blank" class="social-icon" title="TikTok">TK</a>
                    <a href="https://instagram.com" target="_blank" class="social-icon" title="Instagram">IG</a>
                </div>
            </div>
        </header>

        <section id="inicio" class="hero">
            <h1>CREATIVE INVASION PROTOCOL</h1>
            <p>DELULU INVADERS es un estudio arcade especializado en la integración de diseño pixelado, desarrollo de sistemas de juego avanzados y análisis de tendencias gaming para la creación de experiencias digitales retro y estrategias de marca nostálgicas.</p>
            <div class="game-stats">
                <div class="stat">LIVES: ∞</div>
                <div class="stat">LEVEL: CREATIVE</div>
                <div class="stat">BONUS: ACTIVE</div>
            </div>
        </section>

        <section id="servicios" class="services">
            <h2>ENEMY FORMATIONS</h2>
            <div class="services-grid">
                <div class="service-card">
                    <div class="service-points">100 PTS</div>
                    <div class="service-number">LVL 01</div>
                    <div class="service-title">UI/UX INVADERS</div>
                    <div class="service-description">Desarrollamos interfaces digitales retro y experiencias de usuario que conectan con tu audiencia mediante algoritmos de engagement nostálgico.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">200 PTS</div>
                    <div class="service-number">LVL 02</div>
                    <div class="service-title">QA DEFENDERS</div>
                    <div class="service-description">Garantizamos la excelencia en cada proyecto mediante protocolos rigurosos de testing y debugging automatizado estilo arcade.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">300 PTS</div>
                    <div class="service-number">LVL 03</div>
                    <div class="service-title">TREND SCOUTS</div>
                    <div class="service-description">Anticipamos tendencias futuras utilizando análisis de patrones gaming, machine learning retro y tecnología de predicción pixelada.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">400 PTS</div>
                    <div class="service-number">LVL 04</div>
                    <div class="service-title">BRAND BOSSES</div>
                    <div class="service-description">Construimos identidades digitales sólidas que reflejan la esencia única de tu marca en el ecosistema gaming retro.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">500 PTS</div>
                    <div class="service-number">LVL 05</div>
                    <div class="service-title">PIXEL ARTISTS</div>
                    <div class="service-description">Comunicamos visualmente tus ideas mediante diseños pixelados impactantes y sistemas gráficos memorables estilo 8-bit.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">600 PTS</div>
                    <div class="service-number">LVL 06</div>
                    <div class="service-title">SOCIAL WARRIORS</div>
                    <div class="service-description">Desarrollamos estrategias de contenido viral que generan engagement orgánico y construyen comunidades gaming digitales.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">700 PTS</div>
                    <div class="service-number">LVL 07</div>
                    <div class="service-title">PHOTO SHOOTERS</div>
                    <div class="service-description">Capturamos la esencia de tu marca a través de fotografía digital profesional con técnicas de post-procesamiento retro.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">800 PTS</div>
                    <div class="service-number">LVL 08</div>
                    <div class="service-title">STORY CODERS</div>
                    <div class="service-description">Creamos narrativas digitales persuasivas que conectan emocionalmente con tu audiencia mediante storytelling algorítmico arcade.</div>
                </div>

                <div class="service-card">
                    <div class="service-points">1000 PTS</div>
                    <div class="service-number">BOSS</div>
                    <div class="service-title">DIGITAL MAGAZINE</div>
                    <div class="service-description">Publicamos insights y tendencias emergentes a través de nuestra revista digital especializada en innovación gaming y cultura retro.</div>
                </div>
            </div>
        </section>

        <section id="contacto" class="cta">
            <h2>FINAL BOSS BATTLE</h2>
            <p>¿Listo para enfrentar el desafío creativo definitivo? Transformemos tus ideas en experiencias digitales que marquen un nuevo high score en el mercado.</p>
            <a href="#contacto" class="cta-button">INSERT COIN</a>
        </section>

        <footer>
            <div class="footer-score">GAME OVER - FINAL SCORE: INFINITE</div>
            <p>© 2024 DELULU INVADERS ARCADE STUDIO | PRESS START TO CONTINUE</p>
        </footer>
    </div>

    <script>
        // Create starfield
        function createStarfield() {
            const starfield = document.querySelector('.starfield');
            const numStars = 100;
            
            for (let i = 0; i < numStars; i++) {
                const star = document.createElement('div');
                star.className = 'star';
                star.style.left = Math.random() * 100 + '%';
                star.style.top = Math.random() * 100 + '%';
                star.style.width = Math.random() * 3 + 1 + 'px';
                star.style.height = star.style.width;
                star.style.animationDelay = Math.random() * 2 + 's';
                starfield.appendChild(star);
            }
        }

        // Mobile menu toggle
        const mobileMenuToggle = document.querySelector('.mobile-menu-toggle');
        const mobileMenu = document.querySelector('.mobile-menu');

        mobileMenuToggle.addEventListener('click', function() {
            mobileMenuToggle.classList.toggle('active');
            mobileMenu.classList.toggle('active');
        });

        // Close mobile menu when clicking on a link
        const mobileNavLinks = document.querySelectorAll('.mobile-nav-menu a');
        mobileNavLinks.forEach(link => {
            link.addEventListener('click', function() {
                mobileMenuToggle.classList.remove('active');
                mobileMenu.classList.remove('active');
            });
        });

        // Smooth scrolling for navigation links
        const navLinks = document.querySelectorAll('a[href^="#"]');
        navLinks.forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const targetSection = document.querySelector(targetId);
                
                if (targetSection) {
                    const headerHeight = document.querySelector('header').offsetHeight;
                    const targetPosition = targetSection.offsetTop - headerHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // CTA button with arcade sound simulation
        document.querySelector('.cta-button').addEventListener('click', function(e) {
            e.preventDefault();
            
            // Simulate arcade sounds in console
            console.log('🔊 BEEP BEEP BEEP!');
            console.log('🎮 COIN INSERTED');
            console.log('🚀 STARTING NEW GAME...');
            
            alert('🎮 GAME START!\n\n¡Gracias por insertar tu moneda!\nPronto implementaremos el sistema de contacto arcade.\n\nPRESS START TO CONTINUE...');
        });

        // Service card hover effects with sound simulation
        const serviceCards = document.querySelectorAll('.service-card');
        serviceCards.forEach((card, index) => {
            card.addEventListener('mouseenter', function() {
                console.log(`🎯 TARGET ACQUIRED: ${card.querySelector('.service-title').textContent}`);
                console.log('💥 PEW PEW PEW!');
            });
            
            card.addEventListener('click', function() {
                console.log(`💥 ENEMY DESTROYED! +${card.querySelector('.service-points').textContent}`);
            });
        });

        // Close mobile menu when clicking outside
        document.addEventListener('click', function(e) {
            if (!mobileMenuToggle.contains(e.target) && !mobileMenu.contains(e.target)) {
                mobileMenuToggle.classList.remove('active');
                mobileMenu.classList.remove('active');
            }
        });

        // Active navigation highlighting
        window.addEventListener('scroll', function() {
            const sections = document.querySelectorAll('section[id]');
            const navLinks = document.querySelectorAll('.nav-menu a, .mobile-nav-menu a');
            
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                if (pageYOffset >= (sectionTop - 200)) {
                    current = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === '#' + current) {
                    link.classList.add('active');
                }
            });
        });

        // Simulate invader movement
        setInterval(() => {
            const cards = document.querySelectorAll('.service-card');
            cards.forEach((card, index) => {
                const randomMove = Math.random() > 0.7;
                if (randomMove) {
                    card.style.transform += ` translateX(${Math.random() * 4 - 2}px)`;
                    setTimeout(() => {
                        card.style.transform = card.style.transform.replace(/translateX\([^)]*\)/g, '');
                    }, 200);
                }
            });
        }, 2000);

        // Game boot sequence
        window.addEventListener('load', function() {
            createStarfield();
            
            console.log('%c🎮 DELULU INVADERS ARCADE SYSTEM BOOT', 'color: #ffff00; font-family: monospace; font-size: 16px; font-weight: bold;');
            console.log('%c🚀 LOADING CREATIVE MODULES...', 'color: #00ff00; font-family: monospace; font-size: 12px;');
            console.log('%c👾 ENEMY FORMATIONS READY', 'color: #ff0080; font-family: monospace; font-size: 12px;');
            console.log('%c🎯 SYSTEM READY - PRESS START!', 'color: #00ffff; font-family: monospace; font-size: 12px;');
            
            // Simulate coin insert sound
            setTimeout(() => {
                console.log('%c🪙 INSERT COIN TO CONTINUE...', 'color: #ffff00; font-family: monospace; font-size: 14px; animation: blink 1s infinite;');
            }, 2000);
        });

        // Random glitch effects
        setInterval(() => {
            const elements = document.querySelectorAll('.service-card, .logo, .cta h2');
            const randomElement = elements[Math.floor(Math.random() * elements.length)];
            
            randomElement.style.filter = 'hue-rotate(180deg)';
            setTimeout(() => {
                randomElement.style.filter = 'none';
            }, 100);
        }, 8000);
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'994eefb55742f6a5',t:'MTc2MTUzMzk1NS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
